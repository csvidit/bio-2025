import { Inter, Instrument_Serif, Gantari } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const instrumentSerif = Instrument_Serif({
    subsets: ["latin"],
    weight: "400"
});

export const gantari = Gantari({
    subsets: ["latin"],
    weight: "400"
});
